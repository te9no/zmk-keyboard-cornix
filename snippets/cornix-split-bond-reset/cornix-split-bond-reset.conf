# Transitional recovery image only. Preserve host profiles while removing all
# saved split peripheral addresses and their Bluetooth keys. Both peripherals
# must be reset and paired again after using this image.
CONFIG_CORNIX_SPLIT_BOND_RESET=y
